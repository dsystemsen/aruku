# Python（旧ジェネレータの実行時生成物）
__pycache__/
*.pyc

# OS / OneDrive
.DS_Store
Thumbs.db
desktop.ini

# Claude Code のローカル個人設定（共有しない）
.claude/settings.local.json

# CMS のコンテンツ実体（サーバ側がデータ所有者。git/デプロイで上書きしない）
# ※ articles.php が初回シードの元データ。data/content.json は本番で生成・編集される。
data/

# 本番DB(MySQL)接続情報（秘密情報。git管理せず、サーバ上に直接設置）
inc/db_config.php

# 開発用ルータ・SQLite（ローカル専用。本番へは送らない）
dev-router.php

# 会員投稿のアップロード画像（ユーザーコンテンツ。dir と .htaccess だけ追跡）
uploads/*
!uploads/.htaccess

# コンテンツ移行SQL（会員パスワードハッシュ＋全コラム本文を含む＝git管理しない。手動で本番投入）
migrations/*.sql

